## Agent skills

### Issue tracker

问题（issues）以 markdown 文件形式存放在 `.scratch/<feature>/` 下。详见 `docs/agents/issue-tracker.md`。

### Domain docs

单上下文（single-context）——仓库根目录放一个 `CONTEXT.md` 和一个 `docs/adr/`。详见 `docs/agents/domain.md`。
