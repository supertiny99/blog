---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={`关于 | ${SITE_TITLE}`} description="关于「行路的日记」与作者" />
		<style>
			.intro {
				margin: 4rem 0 3rem;
			}
			.intro h1 {
				margin: 0 0 0.5em;
			}
			.intro p {
				margin: 0;
				color: rgb(var(--gray));
				font-size: 1.1rem;
			}
			.prose p:last-child {
				margin-bottom: 0;
			}
			.section-title {
				display: flex;
				align-items: center;
				gap: 1rem;
				margin: 2.5rem 0 1rem;
				font-size: 0.9rem;
				color: rgb(var(--gray));
				letter-spacing: 0.1em;
			}
			.section-title::after {
				content: '';
				flex: 1;
				border-bottom: 1px solid rgb(var(--gray), 0.3);
			}
			.link-list {
				list-style: none;
				margin: 0;
				padding: 0;
			}
			.link-list li {
				border-bottom: 1px solid rgb(var(--gray), 0.15);
			}
			.link-list a {
				display: flex;
				align-items: baseline;
				justify-content: space-between;
				gap: 1rem;
				padding: 0.85em 0.2em;
				text-decoration: none;
			}
			.link-list .link-title {
				color: rgb(var(--black));
				font-weight: bold;
			}
			.link-list a:hover .link-title {
				color: var(--accent);
			}
			.link-list .link-arrow {
				color: rgb(var(--gray));
				font-size: 0.85rem;
			}
			@media (max-width: 720px) {
				.intro {
					margin: 2.5rem 0 2rem;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section class="intro">
				<h1>关于</h1>
				<p>关于「行路的日记」，和写它的这个人。</p>
			</section>
			<section class="prose">
				<p>
					「行路的日记」是一个个人博客。之所以叫「行路」，是因为学习和生活都像走路——重要的不是走得多快，而是一路上留下了什么。这里的文章，就是我在路上捡起的石子。
				</p>
				<p>
					博客里大部分内容是学习笔记和实践记录——遇到感兴趣的东西就动手试一试，把过程和踩过的坑写下来。也偶尔写点与技术无关的日常。
				</p>
				<p>如果这些文字恰好帮到了你，或者想聊聊某个话题，欢迎来 GitHub 找我。</p>
			</section>
			<section>
				<h2 class="section-title">找到我</h2>
				<ul class="link-list">
					<li>
						<a href="https://github.com/supertiny99">
							<span class="link-title">GitHub</span>
							<span class="link-arrow">↗</span>
						</a>
					</li>
				</ul>
			</section>
		</main>
		<Footer />
	</body>
</html>
